import './styles.css';

export default function ContentMain() {

    return (
        <section className="title-welcome-container">
            <h2 className="title-welcome">Bem-vindos!</h2>
        </section>
    );
}